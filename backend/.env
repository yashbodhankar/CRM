MONGODB_URI=mongodb+srv://yash:Yash123@cluster0.koowd8l.mongodb.net/?appName=Cluster0
PORT=5003

# Development: set to true to skip MongoDB and allow dev login (uses ADMIN_EMAIL/ADMIN_PASSWORD)
DISABLE_AUTH=true

# JWT secret used to sign auth tokens
JWT_SECRET=change_this_in_production

# Comma-separated list of allowed frontend origins (leave empty in local dev if needed)
CORS_ORIGIN=http://localhost:3000

# Absolute directory path used to persist uploaded files
UPLOADS_DIR=uploads

# Seed an initial admin user on startup
SEED_ADMIN=true
ADMIN_NAME=Admin User
ADMIN_EMAIL=YASHBODHANKAR6@GMAIL.COM
ADMIN_PASSWORD=admin123
ADMIN_ROLE=admin
